Create Table  node.Team (
    `ID` INT AUTO_INCREMENT NOT NULL  COMMENT 'ID',
    `NAME` VARCHAR(20) NOT NULL  COMMENT 'NAME',
    PRIMARY KEY (`ID`)
)engine=innodb default charset=utf8 comment 'TEAM';
